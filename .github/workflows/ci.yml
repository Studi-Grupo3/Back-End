name: Call Run Unit Tests

on:
  push:
    branches:
      - actions/pipeline
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  run-tests:
    uses: Studi-Grupo3/Back-End/.github/workflows/ci-main.yml@actions/pipeline
